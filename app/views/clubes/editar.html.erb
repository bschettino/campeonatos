<h1>Editar Clube</h1>


<%= form_tag(atualizar_clube_path) do %>
  <% if @clube.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clube.errors.count, "erro") %> <%= pluralize(@clube.errors.count, "impede") %> que este clube seja atualizado:</h2>
      <ul>
        <% @clube.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= label_tag :nome %>
    <%= text_field_tag "clube[nome]" %>
  </div>
    <%= hidden_field_tag :id, @clube.id %>

  <div class="actions">
    <%= submit_tag "Atualizar Clube", :class => "medium blue button" %>
  </div>
<% end %>


